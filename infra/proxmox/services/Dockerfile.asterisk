FROM ubuntu:24.04
ENV DEBIAN_FRONTEND=noninteractive
RUN apt-get update && apt-get install -y --no-install-recommends asterisk python3 python3-websockets curl sox && rm -rf /var/lib/apt/lists/*
CMD ["/usr/sbin/asterisk", "-f"]
